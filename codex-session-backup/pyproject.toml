[project]
name = "codex-session-backup"
version = "0.1.0"
description = "Catalog Codex sessions, select backup/delete candidates, and back rollouts up with compression."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
csb = "codex_session_backup.cli:main"

[build-system]
requires = ["uv_build>=0.12,<0.13"]
build-backend = "uv_build"
